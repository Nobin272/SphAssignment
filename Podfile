# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

use_frameworks!

target 'SphAssignment' do
  # Pods for SPH

  #networking
  pod 'SwiftyJSON', '~> 4.0'
  pod 'UIScrollView-InfiniteScroll', '~> 1.1.0'
end

target 'SphAssignmentTests' do
    inherit! :search_paths
    # Pods for testing
    
    #networking
    #pod 'SwiftyJSON', '~> 4.0'
    pod 'UIScrollView-InfiniteScroll', '~> 1.1.0'
    
end

target 'SphAssignmentUITests' do
    inherit! :search_paths
    # Pods for testing
    
    #networking
    #pod 'SwiftyJSON', '~> 4.0'
    pod 'UIScrollView-InfiniteScroll', '~> 1.1.0'
    
end
